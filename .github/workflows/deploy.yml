name: Deploy to Railway

# Railway auto-deploys from GitHub natively via webhook.
# This file exists as a placeholder — no action needed here.

on:
  push:
    branches:
      - main

jobs:
  check:
    name: Confirm push received
    runs-on: ubuntu-latest
    steps:
      - name: OK
        run: echo "Push received — Railway will deploy automatically."
